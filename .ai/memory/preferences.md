# Preferences Memory

- Seth prefers strict, stepwise execution with no skipped workflow steps.
- When plan instructions conflict with real SDK APIs, raise mismatch explicitly and proceed with technically sound adaptation.
- Keep secrets out committed changes; use `.env.example` template and request real values only when runtime verification requires them.
